export enum SlotType {
  CUSTOM = 'CUSTOM',
  RECURRING = 'RECURRING',
}

export enum SlotMode {
  WAVE = 'WAVE',
  STREAM = 'STREAM',
}

export enum SessionType {
  MORNING = 'MORNING',
  AFTERNOON = 'AFTERNOON',
  EVENING = 'EVENING',
}